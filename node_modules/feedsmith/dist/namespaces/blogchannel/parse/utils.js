import { isObject, parseSingularOf, parseString, retrieveText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/blogchannel/parse/utils.ts
const retrieveFeed = (value) => {
	if (!isObject(value)) return;
	return trimObject({
		blogRoll: parseSingularOf(value["blogchannel:blogroll"], (value$1) => parseString(retrieveText(value$1))),
		blink: parseSingularOf(value["blogchannel:blink"], (value$1) => parseString(retrieveText(value$1))),
		mySubscriptions: parseSingularOf(value["blogchannel:mysubscriptions"], (value$1) => parseString(retrieveText(value$1)))
	});
};

//#endregion
export { retrieveFeed };