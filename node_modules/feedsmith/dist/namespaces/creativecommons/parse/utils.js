import { isObject, parseArrayOf, parseString, retrieveText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/creativecommons/parse/utils.ts
const retrieveItemOrFeed = (value) => {
	if (!isObject(value)) return;
	return trimObject({ licenses: parseArrayOf(value["creativecommons:license"], (value$1) => parseString(retrieveText(value$1))) });
};

//#endregion
export { retrieveItemOrFeed };