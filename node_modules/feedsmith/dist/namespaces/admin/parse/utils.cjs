const require_utils = require('../../../common/utils.cjs');

//#region src/namespaces/admin/parse/utils.ts
const retrieveFeed = (value) => {
	if (!require_utils.isObject(value)) return;
	return require_utils.trimObject({
		errorReportsTo: require_utils.parseSingularOf(value["admin:errorreportsto"], (value$1) => require_utils.retrieveRdfResourceOrText(value$1, require_utils.parseString)),
		generatorAgent: require_utils.parseSingularOf(value["admin:generatoragent"], (value$1) => require_utils.retrieveRdfResourceOrText(value$1, require_utils.parseString))
	});
};

//#endregion
exports.retrieveFeed = retrieveFeed;