import { isObject, parseSingularOf, parseString, retrieveRdfResourceOrText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/admin/parse/utils.ts
const retrieveFeed = (value) => {
	if (!isObject(value)) return;
	return trimObject({
		errorReportsTo: parseSingularOf(value["admin:errorreportsto"], (value$1) => retrieveRdfResourceOrText(value$1, parseString)),
		generatorAgent: parseSingularOf(value["admin:generatoragent"], (value$1) => retrieveRdfResourceOrText(value$1, parseString))
	});
};

//#endregion
export { retrieveFeed };