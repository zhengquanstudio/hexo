import { generateEntry as generateEntry$1, generateFeed as generateFeed$1 } from "../../../feeds/atom/generate/utils.js";

//#region src/namespaces/atom/generate/utils.ts
const generateEntry = (entry) => {
	return generateEntry$1(entry, {
		prefix: "atom:",
		asNamespace: true
	});
};
const generateFeed = (feed) => {
	return generateFeed$1(feed, {
		prefix: "atom:",
		asNamespace: true
	})?.feed;
};

//#endregion
export { generateEntry, generateFeed };