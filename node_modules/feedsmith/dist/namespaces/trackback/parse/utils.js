import { isObject, parseArrayOf, parseSingularOf, parseString, retrieveRdfResourceOrText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/trackback/parse/utils.ts
const retrieveItem = (value) => {
	if (!isObject(value)) return;
	return trimObject({
		ping: parseSingularOf(value["trackback:ping"], (value$1) => retrieveRdfResourceOrText(value$1, parseString)),
		abouts: parseArrayOf(value["trackback:about"], (value$1) => retrieveRdfResourceOrText(value$1, parseString))
	});
};

//#endregion
export { retrieveItem };