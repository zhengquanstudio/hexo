import { DateLike } from "../../../common/types.js";

//#region src/namespaces/app/common/types.d.ts
declare namespace AppNs {
  type Control = {
    draft?: boolean;
  };
  type Entry<TDate extends DateLike> = {
    edited?: TDate;
    control?: Control;
  };
}
//#endregion
export { AppNs };