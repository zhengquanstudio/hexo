const require_utils = require('../../../common/utils.cjs');

//#region src/namespaces/app/parse/utils.ts
const parseControl = (value) => {
	if (!require_utils.isObject(value)) return;
	return require_utils.trimObject({ draft: require_utils.parseSingularOf(value["app:draft"], (value$1) => require_utils.parseYesNoBoolean(require_utils.retrieveText(value$1))) });
};
const retrieveEntry = (value) => {
	if (!require_utils.isObject(value)) return;
	return require_utils.trimObject({
		edited: require_utils.parseSingularOf(value["app:edited"], (value$1) => require_utils.parseDate(require_utils.retrieveText(value$1))),
		control: require_utils.parseSingularOf(value["app:control"], parseControl)
	});
};

//#endregion
exports.retrieveEntry = retrieveEntry;