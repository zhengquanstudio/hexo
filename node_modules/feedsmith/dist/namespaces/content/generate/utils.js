import { generateCdataString, isObject, trimObject } from "../../../common/utils.js";

//#region src/namespaces/content/generate/utils.ts
const generateItem = (item) => {
	if (!isObject(item)) return;
	return trimObject({ "content:encoded": generateCdataString(item?.encoded) });
};

//#endregion
export { generateItem };