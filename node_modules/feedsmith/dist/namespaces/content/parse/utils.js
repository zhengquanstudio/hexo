import { isObject, parseSingularOf, parseString, retrieveText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/content/parse/utils.ts
const retrieveItem = (value) => {
	if (!isObject(value)) return;
	return trimObject({ encoded: parseSingularOf(value["content:encoded"], (value$1) => parseString(retrieveText(value$1))) });
};

//#endregion
export { retrieveItem };