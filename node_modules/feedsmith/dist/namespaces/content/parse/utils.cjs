const require_utils = require('../../../common/utils.cjs');

//#region src/namespaces/content/parse/utils.ts
const retrieveItem = (value) => {
	if (!require_utils.isObject(value)) return;
	return require_utils.trimObject({ encoded: require_utils.parseSingularOf(value["content:encoded"], (value$1) => require_utils.parseString(require_utils.retrieveText(value$1))) });
};

//#endregion
exports.retrieveItem = retrieveItem;