import { isObject, parseNumber, parseSingularOf, retrieveText, trimObject } from "../../../common/utils.js";

//#region src/namespaces/geo/parse/utils.ts
const retrieveItemOrFeed = (value) => {
	if (!isObject(value)) return;
	return trimObject({
		lat: parseSingularOf(value["geo:lat"], (value$1) => parseNumber(retrieveText(value$1))),
		long: parseSingularOf(value["geo:long"], (value$1) => parseNumber(retrieveText(value$1))),
		alt: parseSingularOf(value["geo:alt"], (value$1) => parseNumber(retrieveText(value$1)))
	});
};

//#endregion
export { retrieveItemOrFeed };