const require_config = require('../../../common/config.cjs');
const require_utils = require('../../../common/utils.cjs');
const require_utils$1 = require('./utils.cjs');
const require_index = require('../detect/index.cjs');

//#region src/feeds/json/parse/index.ts
const parse = (value, options) => {
	const json = require_utils.parseJsonObject(value);
	if (!require_index.detect(json)) throw new Error(require_config.locales.invalidFeedFormat);
	const parsed = require_utils$1.parseFeed(json, options);
	if (!parsed) throw new Error(require_config.locales.invalidFeedFormat);
	return parsed;
};

//#endregion
exports.parse = parse;